<?php
/**
 * Template part: footer-info
 *
 * Placeholder for future widget zones / dynamic footer content.
 * Currently outputs nothing dynamic — kept as an extension point so
 * child themes or future updates have a stable hook.
 *
 * @package fcalri
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

do_action( 'fcalri_footer_info' );
